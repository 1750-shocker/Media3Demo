## 页面结构
- 顶部栏：`TopAppBar` 标题“我的音乐”，Edge-to-Edge 下容器用 `systemBarsPadding()`。
- 分区标题：“专辑”，位于顶部栏下方，左右留白 16dp。
- 内容区：双列网格专辑卡片（`LazyVerticalGrid(columns=2)`），卡片圆角、阴影与间距与截图一致。

## 专辑卡片样式
- 封面：使用 Glide 加载 `artworkUrl`，无则回退 `iconUri`；圆角 16dp。
- 底部信息区：半透明遮罩层，三行文案：
  - 专辑名（主文案、较大字号）
  - 艺术家（次文案）
  - 年份（若无则省略）
- 点击导航：`navController.navigate("albumDetail/${album.id}")`
- 加 `wzhhh` 日志：图片加载 URL/URI、点击卡片。

## 数据准备
- 复用 `AlbumsViewModel.albums`。
- 新增 `year` 字段：
  - 优先从专辑节点 `extras["year"]` 读取；若当前 JSON 未提供，展示时省略年份行。
- 保留 `artworkUrl` 与 `iconUri` 的回退策略。

## 代码改动
- 更新 `AlbumsViewModel`：为 `AlbumItem` 增加 `year: String?`，从 `MediaDescription.extras` 尝试读取；保留现有字段。
- 重写 `AlbumsScreen`：
  - 顶部栏 + 分区标题 + 双列网格
  - 卡片样式：圆角、遮罩层与文案布局
  - Glide 加载封面与日志
- Edge-to-Edge：根容器与网格使用 `systemBarsPadding()`，整体 `contentPadding = PaddingValues(16.dp)`。

## 验证
- 首页专辑列表样式与截图一致：顶部标题、分区标题、双列卡片与圆角遮罩。
- 点击卡片进入专辑详情；封面正常显示；日志如预期。

## 备选项
- 若需要完全一致的阴影与颜色，可加入自定义渐变遮罩与 Material 色值微调；如你确认，我会再迭代视觉细节。